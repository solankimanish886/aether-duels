# Server-only secret for the AI judge (api/judge.ts). NEVER expose to the client.
# Without it, the judge falls back to a local mock verdict.
ANTHROPIC_API_KEY=

# Optional: pin the judge model (defaults to claude-opus-4-8).
# JUDGE_MODEL=claude-opus-4-8
